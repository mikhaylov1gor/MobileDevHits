<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-feature
        android:name="android.hardware.camera"
        android:required="false" />

    <uses-permission android:name="android.permission.CAMERA" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.MobileDevHits">

        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.provider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>

        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".ChooseFilterActivity"
            android:exported="true" />

        <activity
            android:name=".ColorCorrectionActivity"
            android:exported="true" />

        <activity
            android:name=".CameraActivity"
            android:exported="true" />

        <activity
            android:name=".BaseFiltersActivity"
            android:exported="true" />

        <activity
            android:name=".RotateActivity"
            android:exported="true" />

        <activity
            android:name=".AffineActivity"
            android:exported="true" />

        <activity
            android:name=".BlurActivity"
            android:exported="true" />

        <activity
            android:name=".DiceActivity"
            android:exported="true" />

        <activity
            android:name=".FaceActivity"
            android:exported="true" />

        <activity
            android:name=".RetouchActivity"
            android:exported="true" />

        <activity
            android:name=".ScaleActivity"
            android:exported="true" />

        <activity
            android:name=".VectorEditActivity"
            android:exported="true" />


    </application>

</manifest>
